问财这个是真的难，难在他不断的调用闭包里的东西，让你不清楚每个变量到底是什么。

总结出他常用的两个手段：

手段1：

var Jn;
!function(n) {...此处省略1万行代码}(Jn || (Jn = {}));

通过一个自运行函数，改变Jn的值，调用Jn里的某个方法，让你根本不知道这个方法里的某个参数是哪来的。



手段2：

Qn = (function(){...此处省略1万行代码})();

如果你通过代码跳转跳进来，会发现自己进入了该函数内部（因为Qn是该函数的返回值！），容易迷失自我，只单独把跳转的那行代码拷贝走。



极其残忍！


